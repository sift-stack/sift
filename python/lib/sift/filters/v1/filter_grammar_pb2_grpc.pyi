"""
@generated by mypy-protobuf.  Do not edit manually!
isort:skip_file
"""

import abc
import collections.abc
import grpc
import grpc.aio
import sift.filters.v1.filter_grammar_pb2
import typing

_T = typing.TypeVar("_T")

class _MaybeAsyncIterator(collections.abc.AsyncIterator[_T], collections.abc.Iterator[_T], metaclass=abc.ABCMeta): ...

class _ServicerContext(grpc.ServicerContext, grpc.aio.ServicerContext):  # type: ignore[misc, type-arg]
    ...

class FilterGrammarServiceStub:
    """FilterGrammarService describes the CEL filter capabilities shared by every
    resource: the operators and member functions available for each built-in
    field type, plus the functions usable anywhere in an expression. It does not
    vary by resource, org, or user, so a client fetches it once and caches it,
    then uses it to render and label the per-field tokens returned in FilterField.
    """

    def __init__(self, channel: typing.Union[grpc.Channel, grpc.aio.Channel]) -> None: ...
    GetFilterGrammar: grpc.UnaryUnaryMultiCallable[
        sift.filters.v1.filter_grammar_pb2.GetFilterGrammarRequest,
        sift.filters.v1.filter_grammar_pb2.GetFilterGrammarResponse,
    ]

class FilterGrammarServiceAsyncStub:
    """FilterGrammarService describes the CEL filter capabilities shared by every
    resource: the operators and member functions available for each built-in
    field type, plus the functions usable anywhere in an expression. It does not
    vary by resource, org, or user, so a client fetches it once and caches it,
    then uses it to render and label the per-field tokens returned in FilterField.
    """

    GetFilterGrammar: grpc.aio.UnaryUnaryMultiCallable[
        sift.filters.v1.filter_grammar_pb2.GetFilterGrammarRequest,
        sift.filters.v1.filter_grammar_pb2.GetFilterGrammarResponse,
    ]

class FilterGrammarServiceServicer(metaclass=abc.ABCMeta):
    """FilterGrammarService describes the CEL filter capabilities shared by every
    resource: the operators and member functions available for each built-in
    field type, plus the functions usable anywhere in an expression. It does not
    vary by resource, org, or user, so a client fetches it once and caches it,
    then uses it to render and label the per-field tokens returned in FilterField.
    """

    @abc.abstractmethod
    def GetFilterGrammar(
        self,
        request: sift.filters.v1.filter_grammar_pb2.GetFilterGrammarRequest,
        context: _ServicerContext,
    ) -> typing.Union[sift.filters.v1.filter_grammar_pb2.GetFilterGrammarResponse, collections.abc.Awaitable[sift.filters.v1.filter_grammar_pb2.GetFilterGrammarResponse]]: ...

def add_FilterGrammarServiceServicer_to_server(servicer: FilterGrammarServiceServicer, server: typing.Union[grpc.Server, grpc.aio.Server]) -> None: ...
