name: Sift Protos

on: pull_request
jobs:
  lint-protos:
    - uses: actions/checkout@v2
    - uses: bufbuild/buf-setup-action@v1
    - uses: bufbuild/buf-lint-action@v1
      with:
        input: protos
